export interface Deliverable {
    id: number,
    name: string,
    format: string,
}

export interface DDeliverable {
    deliverableid: number,
    deliverablename: string,
    defaultformat: string,
}